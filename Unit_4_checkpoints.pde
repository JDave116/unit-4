size(600, 600);
background(255);
strokeWeight(3);


// horizontal lines//
for (int x = 0; x <= 600; x+=20){
  line(x,0,x,600);
}


// grid //
//for (int x = 0; x <= width; x += 20) {
//  for ( int y = 0; y <= height; y += 20) {
//    line(x, 0, x, 600);
//    line(0, y, 600, y);
//  }
//}


// diagonal lines //

//for (int x = -600; x < 600; x += 20) {
//  line(x+600, 0, x, 600);
//}

// circles // 

//for (int r = 900; r > 0; r -= 20){
//  circle(300,300,r);
//}

// rectangles //

//for(int x = 600; x > 0; x -= 40){
//  rectMode(CENTER);
//  rect(300,300,x, x-20);
//}

// checkpoint 6//
 //for(int x = 0; x <= 150; x += 10){
  
 //}
